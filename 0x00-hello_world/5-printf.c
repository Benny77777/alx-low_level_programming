#include <stdio.h>
/**
 * main function - A program that prints a line with  printf function
 * Return: 0(Success)
 */
int mian(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
